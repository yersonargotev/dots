version = 1

[commands]
additional_significant = ["pi", "agent", "opencode"]

[labels]
command_format = "directory_and_command"
separator = "  "

[commands.aliases]
"agent" = "cursor"

[directories.aliases]
